name: product_benchmark_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  drift: ^2.28.1
  flutter_bloc: ^8.1.6
  hive_ce: ^2.7.0
  hive_ce_flutter: ^2.3.0
  path: ^1.9.1
  path_provider: ^2.1.2
  sqlite3_flutter_libs: ^0.5.39

dev_dependencies:
  build_runner: ^2.6.0
  drift_dev: ^2.28.1
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
